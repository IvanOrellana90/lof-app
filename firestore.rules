rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir acceso de lectura y escritura a todos los documentos para cualquier usuario autenticado
      allow read, write: if request.auth != null;
    }
  }
}
